<h1>Editing User</h1>

<%= form_for @user, url: admin_user_path(@user), method: :patch do |form| %>
	<%= render partial: 'form', locals: { user: @user, form: form } %>
<% end %>
<%= button_to 'Back', admin_users_path, method: :get, class: "btn btn-outline-dark" %>
